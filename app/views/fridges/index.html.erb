<% @fridges.each do |fridge| %>
    <ul>
    <lh><a href="/fridges/<%= fridge.id %>"><%= fridge.location %> Fridge</a></lh>
      <li>Brand: <%= fridge.brand %></li>
      <li>Size: <%= fridge.size %></li>
      <li><a href='/fridges/<%= fridge.id %>/delete'>Trash the fridge in the <%= fridge.location %> </a></li>
      <li><a href='/fridges/<%= fridge.id %>/edit'>Edit the fridge in the <%= fridge.location %></a></li>
    </ul>
    <br>
  <% end %>
  <hr>
  <a href="/fridges/new">Create Fridge</a>